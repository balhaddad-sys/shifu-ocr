[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "shifu_neural"
version = "0.1.0"
requires-python = ">=3.10"
description = "Rust neural field core for Shifu"

[tool.maturin]
features = ["pyo3/extension-module"]
